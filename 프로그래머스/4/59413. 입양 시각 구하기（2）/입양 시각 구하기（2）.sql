-- 코드를 입력하세요
WITH RECURSIVE H AS(
    SELECT 0 AS hour
    UNION ALL
    SELECT hour+1 FROM H
    WHERE hour<23
)

SELECT H.hour AS HOUR,COUNT(ANIMAL_ID) AS COUNT
FROM H LEFT JOIN ANIMAL_OUTS o on H.hour = HOUR(o.DATETIME)
GROUP BY H.hour
ORDER BY H.hour